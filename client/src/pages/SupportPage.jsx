import React, { useState } from 'react';
import Navbar from '../components/Navbar';
import Sidebar from '../components/Sidebar';
import MobileNav from '../components/MobileNav';

export default function SupportPage() {
  const [formData, setFormData] = useState({ subject: '', message: '' });
  const [submitted, setSubmitted] = useState(false);

  const handleSubmit = (e) => {
    e.preventDefault();
    if (!formData.subject || !formData.message) return;
    
    // Simulate API request
    setTimeout(() => {
      setSubmitted(true);
      setFormData({ subject: '', message: '' });
    }, 1000);
  };

  const faqs = [
    {
      question: 'How accurate are the predictions?',
      answer: 'Our Next Day Stock Price Prediction uses a highly trained XGBRegressor model, achieving approximately 98% tracking accuracy on large cap index data based on historical trends. However, please remember all predictions involve risk.'
    },
    {
      question: 'What do Buy/Sell Signals mean?',
      answer: 'Buy and Sell signals are generated by an XGBClassifier. When expected daily returns cross algorithmic thresholds (+0.5% or -0.5%), a Buy or Sell signal is produced. Otherwise, it advises to Hold.'
    },
    {
      question: 'Can I connect a live brokerage account?',
      answer: 'Currently, StockAI is for informational and educational purposes only. Direct brokerage API integration is planned for a future update.'
    },
    {
      question: 'How often is data refreshed?',
      answer: 'Data is refreshed at the close of every trading day using live feeds from Yahoo Finance.'
    }
  ];

  return (
    <div className="app-shell">
      <Navbar />
      <div className="app-body">
        <Sidebar />
        <main className="app-main custom-scrollbar">
          <div style={{ maxWidth: '900px', margin: '0 auto', width: '100%', paddingBottom: '80px', marginTop: '2rem' }}>
            {/* Header */}
            <div style={{ marginBottom: '40px', textAlign: 'center' }}>
              <h1 style={{ 
                fontSize: '2.5rem', 
                color: '#fff', 
                marginBottom: '12px',
                fontFamily: 'Space Grotesk, sans-serif'
              }}>
                Help & Support
              </h1>
              <p style={{ color: '#849396', fontSize: '1rem', lineHeight: '1.6' }}>
                Need assistance with your StockAI account or exploring our prediction models?
                <br /> We're here to help.
              </p>
            </div>

            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))', gap: '32px' }}>
              
              {/* Contact Form */}
              <div style={{
                background: '#13161f',
                border: '1px solid rgba(255,255,255,0.05)',
                borderRadius: '16px',
                padding: '32px',
                boxShadow: '0 8px 32px rgba(0,0,0,0.4)',
              }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '24px' }}>
                  <span className="material-symbols-outlined" style={{ color: '#00E5FF', fontSize: '28px' }}>support_agent</span>
                  <h2 style={{ color: '#fff', fontSize: '1.25rem', margin: 0 }}>Contact Support</h2>
                </div>

                {submitted ? (
                  <div style={{
                    background: 'rgba(0,229,255,0.1)',
                    border: '1px solid rgba(0,229,255,0.3)',
                    padding: '24px',
                    borderRadius: '12px',
                    textAlign: 'center'
                  }}>
                    <span className="material-symbols-outlined" style={{ color: '#00E5FF', fontSize: '48px', marginBottom: '16px' }}>check_circle</span>
                    <h3 style={{ color: '#fff', margin: '0 0 8px 0', fontSize: '1.1rem' }}>Message Sent!</h3>
                    <p style={{ color: '#bac9cc', margin: 0, fontSize: '0.95rem' }}>We've received your request and will get back to you within 24 hours.</p>
                    <button 
                      onClick={() => setSubmitted(false)}
                      style={{ 
                        marginTop: '24px', background: 'transparent', border: '1px solid #00E5FF', 
                        color: '#00E5FF', padding: '8px 24px', borderRadius: '8px', cursor: 'pointer',
                        transition: 'all 0.2s'
                      }}
                      onMouseEnter={(e) => { e.currentTarget.style.background = 'rgba(0,229,255,0.1)'; }}
                      onMouseLeave={(e) => { e.currentTarget.style.background = 'transparent'; }}
                    >
                      Send Another
                    </button>
                  </div>
                ) : (
                  <form onSubmit={handleSubmit} style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>
                    <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
                      <label style={{ color: '#dfe2f1', fontSize: '0.9rem', fontWeight: '500' }}>Subject</label>
                      <input 
                        type="text" 
                        value={formData.subject}
                        onChange={(e) => setFormData({...formData, subject: e.target.value})}
                        required
                        placeholder="e.g. Model Accuracy"
                        style={{
                          background: '#1c1f2a', border: '1px solid rgba(255,255,255,0.1)',
                          color: '#fff', padding: '12px 16px', borderRadius: '8px',
                          outline: 'none', transition: 'border-color 0.2s'
                        }}
                        onFocus={e => e.currentTarget.style.borderColor = '#00E5FF'}
                        onBlur={e => e.currentTarget.style.borderColor = 'rgba(255,255,255,0.1)'}
                      />
                    </div>
                    <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
                      <label style={{ color: '#dfe2f1', fontSize: '0.9rem', fontWeight: '500' }}>How can we help?</label>
                      <textarea 
                        rows="5"
                        value={formData.message}
                        onChange={(e) => setFormData({...formData, message: e.target.value})}
                        required
                        placeholder="Describe your issue or question..."
                        style={{
                          background: '#1c1f2a', border: '1px solid rgba(255,255,255,0.1)',
                          color: '#fff', padding: '12px 16px', borderRadius: '8px',
                          outline: 'none', resize: 'vertical', minHeight: '120px', transition: 'border-color 0.2s'
                        }}
                        onFocus={e => e.currentTarget.style.borderColor = '#00E5FF'}
                        onBlur={e => e.currentTarget.style.borderColor = 'rgba(255,255,255,0.1)'}
                      />
                    </div>
                    <button 
                      type="submit"
                      style={{
                        background: '#00E5FF', color: '#001f24', border: 'none',
                        padding: '14px', borderRadius: '8px', fontWeight: '700',
                        fontSize: '1rem', cursor: 'pointer', marginTop: '8px',
                        transition: 'transform 0.2s, box-shadow 0.2s',
                        boxShadow: '0 0 15px rgba(0,229,255,0.2)'
                      }}
                      onMouseEnter={(e) => {
                        e.currentTarget.style.transform = 'translateY(-2px)';
                        e.currentTarget.style.boxShadow = '0 0 25px rgba(0,229,255,0.4)';
                      }}
                      onMouseLeave={(e) => {
                        e.currentTarget.style.transform = 'translateY(0)';
                        e.currentTarget.style.boxShadow = '0 0 15px rgba(0,229,255,0.2)';
                      }}
                    >
                      Send Message
                    </button>
                  </form>
                )}
              </div>

              {/* FAQs */}
              <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
                <h2 style={{ color: '#fff', fontSize: '1.25rem', margin: '0 0 8px 0', display: 'flex', alignItems: 'center', gap: '8px' }}>
                  <span className="material-symbols-outlined" style={{ color: '#00E5FF' }}>quiz</span> 
                  Frequently Asked Questions
                </h2>
                
                {faqs.map((faq, index) => (
                  <div key={index} style={{
                    background: '#13161f',
                    border: '1px solid rgba(255,255,255,0.05)',
                    borderRadius: '12px',
                    padding: '20px',
                    transition: 'border-color 0.2s'
                  }}>
                    <h4 style={{ color: '#dfe2f1', margin: '0 0 12px 0', fontSize: '1rem', lineHeight: '1.4' }}>
                      {faq.question}
                    </h4>
                    <p style={{ color: '#849396', margin: 0, fontSize: '0.9rem', lineHeight: '1.6' }}>
                      {faq.answer}
                    </p>
                  </div>
                ))}
              </div>
            </div>
            
          </div>
        </main>
      </div>
      <MobileNav />
    </div>
  );
}
